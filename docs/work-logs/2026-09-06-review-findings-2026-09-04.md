# 工作记录：修复 2026-09-04 审查中的 10 项发现（另有 1 项回滚）

- **日期：** 2026-09-06
- **分支：** `fix/review-findings-2026-09-04`（基于 `main` 的 `bad5d7e`）
- **关联文档：** [审查归档](../code-reviews/2026-09-04-ui-redundancy-performance.md)、
  [审查当天的工作记录](2026-09-04-ui-redundancy-performance.md)

## 一、用户的请求

> @docs/work-logs/2026-09-04-ui-redundancy-performance.md
> @docs/code-reviews/2026-09-04-ui-redundancy-performance.md ,I'll review the code review
> from September 4th to see what needs fixing and optimization. After fixing the code, I'll
> build and test it myself before submitting a pull request.

即：按 2026-09-04 的审查报告修代码；改完由用户自己构建、实测，通过后再提 PR。

范围经询问后确定：

- **修 11 项**：1、2、4、5、6、7、8、9、10、11、12；
- **不修发现 3**（搜索结果渐进式展示）：它会改动 SearchManager 的回调契约，
  应该单独一轮、单独测；
- **不动「额外优化候选」**：审查报告自己就写着要先测量，而目前没有任何测量数据；
- **一个 PR，一项发现一个提交。**

## 二、动手前的复核

审查报告由 Codex 生成，所以先逐条对着当前 `main`（`25bed12`）核了一遍，
**12 项全部仍然可复现**。同时发现两点报告没有提到、并且直接改变了做法的事实：

1. **`getModifierDetail()` 全项目只有一个调用方**（`src/Backend.cpp:393`），
   所以它的回调契约可以放心改。
2. **ModifierManager 那份 INI 库，应用里根本没人读。** Backend 只用到它的
   `getModifierDetail` 和 `downloadModifier`；`src/`、`tests/`、`qml/` 里没有任何地方
   调用 `getDownloadedModifiers()` 或那几个更新检查函数。
   所以发现 4 **不需要**报告里建议的重构加 INI/JSON 迁移，只要把多余的那次写入删掉。

## 三、具体改了哪些文件

一项发现一个提交，共 11 个提交（外加测试与文档）：

| 提交 | 发现 | 改动 |
|------|------|------|
| `4d14a31` | 1、2 | 详情请求加代号并校验 URL，过期回复直接丢弃；`getModifierDetail` 明确回报成败；Backend 新增 `detailState` 属性 |
| `0e7270d` | 4、12 | ModifierManager 不再写库；Backend 改为按「名称+版本」upsert；模型新增 `updateModifier` 发 `dataChanged` |
| `e476eaf` | 11 | 排序方式存进 `m_sortOrder`，抽出 `applySortOrder()`，新结果到达时一并应用 |
| `44adc46` | 9 | `deleteModifier` 改为返回 bool，删除失败时保留记录并发 `deleteFailed` 信号 |
| `6de8a66` | 10 | 新增 `openModifierFolder(index)` 与 `openTaskFolder(taskId)`，按条目自身路径定位 |
| `0ce8c26` | 7 | SearchPage 统一到一个 `submitSearch()` |
| `ae11700` | 8 | 排队中的任务改为显示「暂停」并调用 `pauseDownload()` |
| `5db9c25` | 5 | IconButton 改基于 `AbstractButton`；抽出 `SettingsNavItem`；三个 Styled 控件补 `visualFocus`（**已被 `6298b6b` 回滚**） |
| `3c4cada` | 6 | ThemeProvider 新增 `primaryTextColor` 与 `textMuted` 两个令牌 |
| `5c9254e` | — | 新增单元测试 |
| `dc0dcd9` | — | 先原样归档 2026-09-04 的两份文档 |
| `6298b6b` | 5 | **回滚** `5db9c25`，原因见下 |

按文件看（回滚发现 5 之后，相对 `origin/main` 的净改动）：

| 文件 | 改动 | 对应发现 |
|------|------|----------|
| `src/Backend.{h,cpp}` | 详情请求代号与 `detailState`、排序状态、删除失败处理、按条目打开目录、库记录 upsert | 1、2、4、9、10、11、12 |
| `src/ModifierManager.{h,cpp}` | 回调加成败标志；删掉写 `.crdownload` 路径进 INI 的那次调用 | 2、4 |
| `src/DownloadedModifierModel.{h,cpp}` | 新增 `updateModifier()` | 12 |
| `qml/components/DetailDrawer.qml` | 遮罩改由 `detailState` 驱动，关闭按钮移进遮罩，失败给重试，空页面照样显示信息 | 2、6 |
| `qml/components/SettingsDialog.qml` | 说明文字改用 `textMuted` | 6 |
| `qml/components/DownloadListPopup.qml` | 排队任务改用暂停；空状态文字改 `textMuted` | 6、8 |
| `qml/components/StyledTable.qml`、`StyledTextField.qml` | 空状态、占位符改用 `textMuted` | 6 |
| `qml/pages/SearchPage.qml` | 统一提交入口 `submitSearch()` | 7 |
| `qml/pages/DownloadedPage.qml` | 新增删除失败提示条 | 9 |
| `qml/themes/ThemeProvider.qml` | 新增两个文字色令牌 | 6 |
| `qml/Main.qml` | 传入 `detailState` 与重试；选中标签用 `primaryTextColor`；文件夹按钮传索引；接删除失败信号 | 2、6、9、10 |
| `tests/unit/backend_test.cpp` | 5 个新用例 | 1、2、12 |

### 发现 6 的取值依据

用审查报告里同一套（sRGB 线性化相对亮度）公式，对每个主题的 `surfaceColor` 计算，
调到 4.5:1 以上。**填充色一律没动**，只动文字色：

| 主题 | 强调文字 | 原对比度 | 现对比度 | 次要文字 | 原对比度 | 现对比度 |
|------|----------|---------|---------|----------|---------|---------|
| Light | `#2196F3` → `#0B78CF` | 3.12 | 4.56 | `#BDBDBD` → `#757575` | 1.88 | 4.61 |
| Dark | `#64B5F6`（保持） | 6.50 | 6.50 | `#546E7A` → `#82949E` | 2.66 | 4.58 |
| Ocean | `#00BCD4` → `#008292` | 2.30 | 4.56 | `#80CBC4` → `#00838F` | 1.87 | 4.52 |
| Sunset | `#FF7043` → `#DB3400` | 2.67 | 4.54 | `#BCAAA4` → `#926C5F` | 2.17 | 4.51 |
| Forest | `#4CAF50` → `#3A853D` | 2.78 | 4.56 | `#A5D6A7` → `#3C853F` | 1.64 | 4.55 |
| Lavender | `#9C7CF4` → `#8057F1` | 3.18 | 4.61 | `#CE93D8` → `#A551C9` | 2.39 | 4.55 |
| Rose | `#E91E63` → `#E6165D` | 4.35 | 4.53 | `#F8BBD9` → `#D73475` | 1.60 | 4.53 |
| Midnight | `#5C6BC0` → `#7884CB` | 3.27 | 4.51 | `#5C6BC0` → `#7884CB` | 3.27 | 4.63 |
| Mocha | `#8D6E63`（保持） | 4.62 | 4.62 | `#BCAAA4` → `#946E62` | 2.23 | 4.50 |

## 四、验证情况

- **没有构建，没有跑测试。** 改动在 WSL 里完成，`build.cmd` 需要 Windows，
  而这台机器上 WSL interop 起不来 `cmd.exe`（`cmd.exe /c build.cmd tests` 会被当成
  shell 脚本执行，报 `MZ...: not found`）。构建与实测由用户完成。
- 新增的 5 个单元测试只覆盖发现 1、2、12：
  - 用户已经切走之后才回来的详情响应，不会改动当前选中项，也不会改 `detailState`；
  - 详情请求失败进入 `error`，没有详情 URL 进入 `empty`；
  - `updateModifier()` 只发 `dataChanged`、不发 `modelReset`，越界索引被忽略。
- **发现 4、9、10、11 以及全部 QML 改动没有自动化测试**，这是有意为之而不是遗漏：
  它们分别需要沙箱化的数据目录、一个能构造删除失败的文件、或者一段解析器认得的
  搜索结果 HTML；仓库里也完全没有 Qt Quick 的测试环境。宁可写明没覆盖，
  也不写一个测不到点子上的用例。
- 没有用 `QSignalSpy`：它会把 `Qt::Test` 引进一个没有链接它的目标。

需要用户执行的验证（大部分改的是界面，只能靠实跑）：

1. `build.cmd tests`。
2. `build.cmd run debug`，重点走这几条：
   - 断网或改坏 URL，确认详情面板给出失败提示且能关掉、能重试；
   - 快速在两个游戏之间来回切换，确认版本列表不会串到另一个游戏上；
   - 排队第二个下载，点它的按钮，确认变成「暂停」而不是没反应；
   - 用别的程序占住一个已下载文件再删除，确认记录还在并弹出提示；
   - 改掉默认下载目录，再点旧记录的文件夹图标，确认打开的是旧目录；
   - 先排序再搜索，确认排序没有被新结果冲掉；
   - 重复下载同一版本，确认列表里只有一条记录；
   - 九个主题逐个看选中标签和占位符文字。

## 五、发现 5 被回滚

用户构建后发现**全应用的图标都被撑大了**（标题栏的下载/设置/最小化/最大化/关闭，
以及详情面板的关闭和下载图标），并附了两张截图。

原因很直接：`AbstractButton` 是 `Control`，它会把 `contentItem` 拉伸到整个内容区，
所以那张 `Image` 不再停在 `iconSize`，而是被 `fillMode` 放大到填满按钮。
旧实现是 `Rectangle` + 居中的定尺寸 `Image`，所以没有这个问题。

**整个提交回滚，而不是就地打补丁。** 尺寸问题本身好修（把 Image 包一层 `Item`
并给死 `width`/`height`），但这需要一次构建来确认，而当下更要紧的是先把可用的界面
交回去。无障碍的思路本身没问题，重做时请照审查归档里那条「Attempted and reverted」
的提示来，并且一定要在真实构建里同时看标题栏和列表行。

回滚是按文件反向合并做的，因此**同一批文件里属于其他发现的改动都保留了**：
详情面板的 `detailState` 遮罩与重试（发现 2）、已下载页的删除失败提示条（发现 9）、
以及 `textMuted` / `primaryTextColor` 配色（发现 6）都还在。

这次也说明了一件事：本轮所有 QML 改动都没有自动化测试兜底，
发现 5 正是这个缺口的代价。

## 六、遗留事项

- **发现 5 回到未实施状态**，审查归档已改回 ❌ 并写明原因与重做建议。
- **发现 3 仍未实施**，审查归档里已按「❌ No - deferred」记录并写明原因。
- 「额外优化候选」全部未动：需要先测量再决定，本次不提供任何虚构的提速数字。
- `SearchManager::loadFeaturedModifiers()` 现在已经不会被界面触达。
  是删掉它、还是把两套首页解析合并，是单独的决定，本次没有顺手做。
- 读屏软件的实际播报效果没有验证：本次只补了 `Accessible.role` 与
  `Accessible.name`，没有在 Windows 上用真正的读屏软件听过。
- 发现 9 提到的「删除前确认或可撤销删除」属于产品决策，未实施。
- 工作区里 `CMakeLists.txt` 第 178 行 `WIN32` 被注释掉的本地调试改动**没有提交**，
  本次只提交了 `SettingsNavItem.qml` 那一行。
- PR 记录等用户实测通过后再写，那时「怎么验证」几个勾才能如实填写。
